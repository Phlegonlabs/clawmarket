# System Overview

The project uses `.harness/state.json` as the machine-readable state source, `docs/PROGRESS.md` as the session recovery entry point, and `validate.ts` for all gate / guardian validation.
