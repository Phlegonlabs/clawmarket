# Key Decisions

- Use Bun as the package manager
- Use local `.harness/*.ts` as the runtime
- Use dual AGENTS / CLAUDE document sync to maintain multi-toolchain consistency
- Use PRD-driven backlog and worktree planning
